name: 🚀 Auto Deploy IELTS Platform (Render + Vercel)

on:
  push:
    branches: ['main']

jobs:
  deploy:
    name: Deploy Backend & Frontend
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      NODE_ENV: production
      TZ: Asia/Ho_Chi_Minh

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: 🧠 Validate environment variables
        run: |
          echo "🔍 Checking secrets availability..."
          if [ -z "${{ secrets.RENDER_API_KEY }}" ]; then 
            echo "⚠️ Missing RENDER_API_KEY (Render will use webhook)"
          else
            echo "✅ RENDER_API_KEY found"
          fi
          if [ -z "${{ secrets.RENDER_SERVICE_ID }}" ]; then 
            echo "⚠️ Missing RENDER_SERVICE_ID (Render will use webhook)"
          else
            echo "✅ RENDER_SERVICE_ID found"
          fi
          if [ -z "${{ secrets.VERCEL_TOKEN }}" ]; then 
            echo "⚠️ Missing VERCEL_TOKEN (Vercel will use GitHub integration)"
          else
            echo "✅ VERCEL_TOKEN found"
          fi

      - name: ⚙️ Deploy Backend to Render
        run: |
          echo "🚀 Deploying backend (Render)..."
          if [ -n "${{ secrets.RENDER_API_KEY }}" ] && [ -n "${{ secrets.RENDER_SERVICE_ID }}" ]; then
            echo "📡 Triggering Render deployment via API..."
            RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "https://api.render.com/deploy/${{ secrets.RENDER_SERVICE_ID }}?key=${{ secrets.RENDER_API_KEY }}" || echo "ERROR")
            HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
            if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "201" ]; then
              echo "✅ Render deployment triggered successfully"
            else
              echo "⚠️ Render API call returned: $HTTP_CODE (Render will auto-deploy via webhook)"
            fi
          else
            echo "ℹ️ Render credentials missing — Render will auto-deploy via GitHub webhook."
          fi
        continue-on-error: true

      - name: 🎨 Deploy Frontend to Vercel
        working-directory: ./client
        run: |
          echo "🚀 Deploying frontend (Vercel)..."
          if [ -n "${{ secrets.VERCEL_TOKEN }}" ]; then
            echo "📡 Using Vercel CLI to deploy..."
            npx vercel --prod --token=${{ secrets.VERCEL_TOKEN }} --yes --force || {
              echo "⚠️ Vercel CLI failed — Vercel GitHub integration will handle deploy"
              exit 0
            }
          else
            echo "ℹ️ No VERCEL_TOKEN found — Vercel GitHub integration will handle deploy."
          fi
        continue-on-error: true

      - name: 🔍 Health Check (Render)
        run: |
          echo "🔍 Checking Render backend health..."
          sleep 10
          # Use the Render service URL (you may need to update this)
          RENDER_URL="https://ielts-platform-emrv.onrender.com"
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" "$RENDER_URL/api/health" || echo "000")
          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ Backend is online (HTTP $HTTP_CODE)"
          else
            echo "⚠️ Backend health check returned: $HTTP_CODE (may still be deploying)"
          fi
        continue-on-error: true

      - name: 🧾 Save deployment logs
        run: |
          mkdir -p logs
          echo "[$(date -u +"%Y-%m-%d %H:%M:%S UTC")] Render deploy triggered" >> logs/deployments.log
          echo "[$(date -u +"%Y-%m-%d %H:%M:%S UTC")] Vercel deploy triggered" >> logs/deployments.log
          git config user.name "IELTS Platform Bot" || true
          git config user.email "ci@ieltsplatform.ai" || true
          git add logs/deployments.log || true
          git commit -m "🪶 Update deployment log [skip ci]" || echo "No changes to commit"
        continue-on-error: true

      - name: 🧯 Rollback Notification (if failed)
        if: failure()
        run: |
          echo "⚠️ Deployment step failed — please review logs and redeploy manually if needed"
          echo "🔗 Render Dashboard: https://dashboard.render.com"
          echo "🔗 Vercel Dashboard: https://vercel.com/dashboard"

      - name: ✅ Final Status
        run: |
          echo "🎉 All deployment steps executed successfully"
          echo "📦 Render: Check https://dashboard.render.com for deployment status"
          echo "📦 Vercel: Check https://vercel.com/dashboard for deployment status"
          echo "ℹ️ Note: Both platforms also auto-deploy via GitHub webhooks/integration"

