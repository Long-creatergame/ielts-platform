{
  "name": "ielts-platform",
  "version": "1.0.0",
  "description": "IELTS Platform - Full Stack Application",
  "scripts": {
    "dev:full": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "dev:server": "cd server && node index.js",
    "dev:client": "cd client && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
